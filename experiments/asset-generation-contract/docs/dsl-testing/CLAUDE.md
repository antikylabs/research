@AGENTS.md

# Claude Code adapter

Use `AGENTS.md` as the shared durable instruction source. For Goal 1, read `docs/DSL_REDESIGN.md` before `GOAL.md`, implement compiler and library code in `../../packages/contracts/`, keep runnable examples in `../../packages/examples/`, treat `../asset-contract/` as read-only, and finish with evidence for every acceptance criterion.
