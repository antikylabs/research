# Contract DSL agent evaluation

Status: **planning only**

No benchmark runner or external-agent execution is approved from this directory.

- Read the [proposed pipeline and owner decisions](PIPELINE_PLAN.md).
- The prematurely implemented runner, draft tasks, generated files, and local run evidence are
  preserved under [`prototype-quarantine/`](prototype-quarantine/README.md). They are not an active
  npm workspace or an accepted baseline.

The existing empty `contract/`, `outputs/`, and `pipeline/` directories remain untouched pending an
approved package shape.
