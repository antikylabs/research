# Revise Blue Winter Grove

Make one bounded revision to the seeded Blue Winter Grove semantic contract. Preserve all four
reference images, all stable definition and cast keys, every relationship, and every other piece of
scene direction.

Apply these exact changes:

1. Change `roundedGrove.amount` from inclusive 28–42 to inclusive 32–38.
2. Change the `rounded-tree-count` acceptance check from inclusive 28–42 to inclusive 32–38.
3. Add this exact item to `roundedGrove.placement.leave`, after the two existing items:
   `two clear sightlines from the creek into the grove`
4. Add this exact human-review question after the four existing scene review questions:
   `Do two clear sightlines carry the eye from the creek into the grove?`

Only `src/blue-winter-grove/blue-winter-grove.layout.ts` and
`src/blue-winter-grove/blue-winter-grove.contract.ts` are writable. Do not regenerate or edit any
reference image, supporting module, task input, or configuration file.
