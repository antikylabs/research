@AGENTS.md

# Claude Code adapter

Use the shared `AGENTS.md` as the source of durable project instructions.

For this task:

- read `GOAL.md` before editing
- inspect repository conventions before selecting package paths or commands
- use a written plan mapped to acceptance criteria
- keep changes inside the bounded Goal 1 scope
- verify deterministic compiler output with an actual repeated build comparison
- finish with a criterion-by-criterion evidence report
