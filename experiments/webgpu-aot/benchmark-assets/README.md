# Pinned benchmark data

This directory contains only the common Sponza model, textures, and required
attribution used to compare the five demos against identical input data. It is
not a package and contains no loader, shader, pipeline, renderer, frame graph,
runtime code, or benchmark policy.

Each demo owns the complete code path that loads and renders these files.
